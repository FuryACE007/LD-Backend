/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { SMART_SUPPLY_ESCROW_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';
import {
  getConsumableSpecDecoder,
  getConsumableSpecEncoder,
  type ConsumableSpec,
  type ConsumableSpecArgs,
} from '../types';

export const CREATE_JOB_DISCRIMINATOR = new Uint8Array([
  178, 130, 217, 110, 100, 27, 82, 119,
]);

export function getCreateJobDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CREATE_JOB_DISCRIMINATOR);
}

export type CreateJobInstruction<
  TProgram extends string = typeof SMART_SUPPLY_ESCROW_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountCounter extends string | AccountMeta<string> = string,
  TAccountJob extends string | AccountMeta<string> = string,
  TAccountIpltMint extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountCounter extends string
        ? WritableAccount<TAccountCounter>
        : TAccountCounter,
      TAccountJob extends string ? WritableAccount<TAccountJob> : TAccountJob,
      TAccountIpltMint extends string
        ? ReadonlyAccount<TAccountIpltMint>
        : TAccountIpltMint,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateJobInstructionData = {
  discriminator: ReadonlyUint8Array;
  consumables: Array<ConsumableSpec>;
};

export type CreateJobInstructionDataArgs = {
  consumables: Array<ConsumableSpecArgs>;
};

export function getCreateJobInstructionDataEncoder(): Encoder<CreateJobInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['consumables', getArrayEncoder(getConsumableSpecEncoder())],
    ]),
    (value) => ({ ...value, discriminator: CREATE_JOB_DISCRIMINATOR })
  );
}

export function getCreateJobInstructionDataDecoder(): Decoder<CreateJobInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['consumables', getArrayDecoder(getConsumableSpecDecoder())],
  ]);
}

export function getCreateJobInstructionDataCodec(): Codec<
  CreateJobInstructionDataArgs,
  CreateJobInstructionData
> {
  return combineCodec(
    getCreateJobInstructionDataEncoder(),
    getCreateJobInstructionDataDecoder()
  );
}

export type CreateJobAsyncInput<
  TAccountOwner extends string = string,
  TAccountCounter extends string = string,
  TAccountJob extends string = string,
  TAccountIpltMint extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  /** Per-owner counter to auto-derive job seed */
  counter?: Address<TAccountCounter>;
  job: Address<TAccountJob>;
  /** IP License Token mint */
  ipltMint: Address<TAccountIpltMint>;
  systemProgram?: Address<TAccountSystemProgram>;
  consumables: CreateJobInstructionDataArgs['consumables'];
};

export async function getCreateJobInstructionAsync<
  TAccountOwner extends string,
  TAccountCounter extends string,
  TAccountJob extends string,
  TAccountIpltMint extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SMART_SUPPLY_ESCROW_PROGRAM_ADDRESS,
>(
  input: CreateJobAsyncInput<
    TAccountOwner,
    TAccountCounter,
    TAccountJob,
    TAccountIpltMint,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CreateJobInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountCounter,
    TAccountJob,
    TAccountIpltMint,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SMART_SUPPLY_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    counter: { value: input.counter ?? null, isWritable: true },
    job: { value: input.job ?? null, isWritable: true },
    ipltMint: { value: input.ipltMint ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.counter.value) {
    accounts.counter.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([106, 111, 98, 95, 99, 111, 117, 110, 116, 101, 114])
        ),
        getAddressEncoder().encode(expectAddress(accounts.owner.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.counter),
      getAccountMeta(accounts.job),
      getAccountMeta(accounts.ipltMint),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateJobInstructionDataEncoder().encode(
      args as CreateJobInstructionDataArgs
    ),
    programAddress,
  } as CreateJobInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountCounter,
    TAccountJob,
    TAccountIpltMint,
    TAccountSystemProgram
  >);
}

export type CreateJobInput<
  TAccountOwner extends string = string,
  TAccountCounter extends string = string,
  TAccountJob extends string = string,
  TAccountIpltMint extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  /** Per-owner counter to auto-derive job seed */
  counter: Address<TAccountCounter>;
  job: Address<TAccountJob>;
  /** IP License Token mint */
  ipltMint: Address<TAccountIpltMint>;
  systemProgram?: Address<TAccountSystemProgram>;
  consumables: CreateJobInstructionDataArgs['consumables'];
};

export function getCreateJobInstruction<
  TAccountOwner extends string,
  TAccountCounter extends string,
  TAccountJob extends string,
  TAccountIpltMint extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SMART_SUPPLY_ESCROW_PROGRAM_ADDRESS,
>(
  input: CreateJobInput<
    TAccountOwner,
    TAccountCounter,
    TAccountJob,
    TAccountIpltMint,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CreateJobInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountCounter,
  TAccountJob,
  TAccountIpltMint,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SMART_SUPPLY_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    counter: { value: input.counter ?? null, isWritable: true },
    job: { value: input.job ?? null, isWritable: true },
    ipltMint: { value: input.ipltMint ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.counter),
      getAccountMeta(accounts.job),
      getAccountMeta(accounts.ipltMint),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateJobInstructionDataEncoder().encode(
      args as CreateJobInstructionDataArgs
    ),
    programAddress,
  } as CreateJobInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountCounter,
    TAccountJob,
    TAccountIpltMint,
    TAccountSystemProgram
  >);
}

export type ParsedCreateJobInstruction<
  TProgram extends string = typeof SMART_SUPPLY_ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    /** Per-owner counter to auto-derive job seed */
    counter: TAccountMetas[1];
    job: TAccountMetas[2];
    /** IP License Token mint */
    ipltMint: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: CreateJobInstructionData;
};

export function parseCreateJobInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCreateJobInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      counter: getNextAccount(),
      job: getNextAccount(),
      ipltMint: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateJobInstructionDataDecoder().decode(instruction.data),
  };
}
