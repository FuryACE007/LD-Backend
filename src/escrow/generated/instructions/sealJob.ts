/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { SMART_SUPPLY_ESCROW_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SEAL_JOB_DISCRIMINATOR = new Uint8Array([
  180, 43, 107, 59, 160, 118, 65, 200,
]);

export function getSealJobDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SEAL_JOB_DISCRIMINATOR);
}

export type SealJobInstruction<
  TProgram extends string = typeof SMART_SUPPLY_ESCROW_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountJob extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountJob extends string ? WritableAccount<TAccountJob> : TAccountJob,
      ...TRemainingAccounts,
    ]
  >;

export type SealJobInstructionData = { discriminator: ReadonlyUint8Array };

export type SealJobInstructionDataArgs = {};

export function getSealJobInstructionDataEncoder(): FixedSizeEncoder<SealJobInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SEAL_JOB_DISCRIMINATOR })
  );
}

export function getSealJobInstructionDataDecoder(): FixedSizeDecoder<SealJobInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSealJobInstructionDataCodec(): FixedSizeCodec<
  SealJobInstructionDataArgs,
  SealJobInstructionData
> {
  return combineCodec(
    getSealJobInstructionDataEncoder(),
    getSealJobInstructionDataDecoder()
  );
}

export type SealJobInput<
  TAccountOwner extends string = string,
  TAccountJob extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  job: Address<TAccountJob>;
};

export function getSealJobInstruction<
  TAccountOwner extends string,
  TAccountJob extends string,
  TProgramAddress extends Address = typeof SMART_SUPPLY_ESCROW_PROGRAM_ADDRESS,
>(
  input: SealJobInput<TAccountOwner, TAccountJob>,
  config?: { programAddress?: TProgramAddress }
): SealJobInstruction<TProgramAddress, TAccountOwner, TAccountJob> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SMART_SUPPLY_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    job: { value: input.job ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [getAccountMeta(accounts.owner), getAccountMeta(accounts.job)],
    data: getSealJobInstructionDataEncoder().encode({}),
    programAddress,
  } as SealJobInstruction<TProgramAddress, TAccountOwner, TAccountJob>);
}

export type ParsedSealJobInstruction<
  TProgram extends string = typeof SMART_SUPPLY_ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    job: TAccountMetas[1];
  };
  data: SealJobInstructionData;
};

export function parseSealJobInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSealJobInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), job: getNextAccount() },
    data: getSealJobInstructionDataDecoder().decode(instruction.data),
  };
}
